# NovelAI Scene Visualizer — Environment Variables
# Copy this to .env and fill in your credentials.
# .env is gitignored and will not be committed.

# NovelAI login credentials (for auto-login)
NOVELAI_EMAIL=
NOVELAI_PASSWORD=

# Venice AI API key (get from https://venice.ai)
VENICE_API_KEY=

# Pollo AI — no API key needed, uses browser login (Settings > Pollo AI > Login)
